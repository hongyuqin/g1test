<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huf.g1test.mapper.OrderMapper">

    <resultMap id="BaseResultMap" type="com.huf.g1test.entity.Order">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="order_no" property="orderNo" jdbcType="VARCHAR"/>
        <result column="user_id" property="userId" jdbcType="VARCHAR"/>
        <result column="amount" property="amount" jdbcType="DECIMAL"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="VARCHAR"/>
        <result column="update_time" property="updateTime" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="selectByOrderNo" resultMap="BaseResultMap">
        SELECT * FROM `order` WHERE order_no = #{orderNo}
    </select>

    <insert id="insert" parameterType="com.huf.g1test.entity.Order">
        INSERT INTO `order` (order_no, user_id, amount, status, create_time, update_time)
        VALUES (#{orderNo}, #{userId}, #{amount}, #{status}, NOW(), NOW())
    </insert>

    <update id="updateStatus">
        UPDATE `order` SET status = #{status}, update_time = NOW() 
        WHERE order_no = #{orderNo}
    </update>

</mapper> 